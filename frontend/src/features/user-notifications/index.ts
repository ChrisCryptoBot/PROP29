/**
 * User Notifications — accessible only from profile dropdown.
 * Full page at /notifications; unread count and recent preview in sidebar profile menu.
 */
export { default } from './NotificationsPage';
