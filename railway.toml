[build]
builder = "nixpacks"
# buildCommand removed to let Nixpacks auto-detect requirements.txt

[deploy]
# Use absolute path to venv python to guarantee correct environment
startCommand = "/opt/venv/bin/python -m uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
