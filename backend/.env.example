# PROPER 2.9 - Environment Variables Example
# Copy this file to .env and update with your actual values

# Backend Configuration
SECRET_KEY=your-secret-key-here-change-in-production
DATABASE_URL=sqlite:///./proper29.db
ENVIRONMENT=development

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# AI/LLM Configuration
# Choose provider: "openai" or "anthropic"
AI_MODEL_PROVIDER=openai

# OpenAI Configuration (if using OpenAI)
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini

# Anthropic Configuration (if using Anthropic Claude)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here
ANTHROPIC_MODEL=claude-3-5-haiku-20241022

# AI Service Configuration
AI_CONFIDENCE_THRESHOLD=0.7
AI_RATE_LIMIT_PER_MINUTE=60
AI_MAX_RETRIES=3
AI_TIMEOUT_SECONDS=30

# Voice-to-Text Transcription Configuration
# Choose provider: "openai_whisper", "deepgram", or "assemblyai"
TRANSCRIPTION_PROVIDER=openai_whisper

# Deepgram Configuration (if using Deepgram)
# DEEPGRAM_API_KEY=your-deepgram-api-key-here

# AssemblyAI Configuration (if using AssemblyAI)
# ASSEMBLYAI_API_KEY=your-assemblyai-api-key-here

# OpenAI Whisper uses the OPENAI_API_KEY already defined above

# Audio Configuration
MAX_AUDIO_DURATION_SECONDS=300
MAX_AUDIO_FILE_SIZE_MB=25

# Email/Notification Configuration (Future)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASSWORD=your-email-password

# Redis Configuration (Future - for caching)
# REDIS_URL=redis://localhost:6379

# Production Settings
# ENVIRONMENT=production
# DATABASE_URL=postgresql://user:password@localhost/proper29
